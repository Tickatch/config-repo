# ========================================
# User Service 설정
# ========================================

# ========================================
# Prometheus Pushgateway 설정 (내부 IP로 오버라이드)
# ========================================
management:
  prometheus:
    metrics:
      export:
        pushgateway:
          address: 192.168.55.164:9091

# ========================================
# 메시징 설정 (RabbitMQ Exchange)
# ========================================
messaging:
  exchange:
    user: tickatch.user