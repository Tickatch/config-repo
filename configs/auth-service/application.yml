# ========================================
# Auth Service 전용 설정
# ========================================
# Config Server에서 관리되는 auth-service 전용 설정
# 공통 설정(application.yml)을 오버라이드하거나 추가 설정 정의
# ========================================

# ========================================
# JWT 설정 (RS256 - 비대칭키)
# ========================================
jwt:
  issuer: ${JWT_ISSUER:tickatch}
  key-id: ${JWT_KEY_ID:tickatch-auth-key-1}
  key-directory: ${JWT_KEY_DIRECTORY:data/keys}
  # 프로덕션에서는 환경변수로 키 값을 주입 (Base64 인코딩)
  private-key: ${JWT_PRIVATE_KEY:}
  public-key: ${JWT_PUBLIC_KEY:}
  # 또는 파일 경로로 지정
  private-key-path: ${JWT_PRIVATE_KEY_PATH:}
  public-key-path: ${JWT_PUBLIC_KEY_PATH:}
  access-token:
    expiration: ${JWT_ACCESS_EXPIRATION:300}  # 5분 (초 단위)


# ========================================
# 커스텀 OAuth 설정 (OAuthProperties용)
# ========================================
oauth:
  frontend-redirect-url: ${OAUTH_FRONTEND_REDIRECT_URL:http://localhost:3000/oauth/callback}
  providers:
    kakao:
      client-id: ${OAUTH_KAKAO_CLIENT_ID}
      client-secret: ${OAUTH_KAKAO_CLIENT_SECRET}
      redirect-uri: ${OAUTH_BASE_REDIRECT_URI:http://localhost:8090}/api/v1/auth/oauth/kakao/callback
    naver:
      client-id: ${OAUTH_NAVER_CLIENT_ID}
      client-secret: ${OAUTH_NAVER_CLIENT_SECRET}
      redirect-uri: ${OAUTH_BASE_REDIRECT_URI:http://localhost:8090}/api/v1/auth/oauth/naver/callback
    google:
      client-id: ${OAUTH_GOOGLE_CLIENT_ID}
      client-secret: ${OAUTH_GOOGLE_CLIENT_SECRET}
      redirect-uri: ${OAUTH_BASE_REDIRECT_URI:http://localhost:8090}/api/v1/auth/oauth/google/callback
